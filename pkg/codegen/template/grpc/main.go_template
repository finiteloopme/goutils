package main

import (
	server "{{ .FullyQualifiedModuleName}}/internal"
	"github.com/finiteloopme/goutils/pkg/log"
)

func main() {
	log.Info("In main function")
//	Run gRPC Service Only.
	server.RunServer()
	
//	Uncomment to start a REST handler for the gRPC service.
//	Ensure the gRPC server above is not being started
//	server.RunWithHTTPHandler()
}
